package main;
import javax.sound.sampled.*;
import java.io.InputStream;

public class Audio {
    private boolean loop;
    private String ref;

    public static void playAudio(String ref) {
        //InputStream inputStream = getClass().getClassLoader().getResourceAsStream("src/audio/" + ref);
    } // playAudio

}
